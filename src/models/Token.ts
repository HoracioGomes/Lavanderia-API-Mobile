

class Token {
    token: string;
    idUsuario: number

    constructor(token: string, idUsuario: number){
        this.token = token;
        this.idUsuario = idUsuario
    }
}

export default Token
